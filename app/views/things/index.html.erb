<div class= "row">
  <h2>Things</h2>

  <table>
    <thead>
      <th>Description</th>
      <th>Actions</th>
    <thead>

    <tbody>
      <% @things.each do |thing| %>
        <tr>
          <td> <%= thing.description%></td>
          <td>
              <%= link_to '🗑', thing_path(thing), data: {
                  turbo_method: :delete,
                  turbo_confirm: "Are you sure?"
              } %>
          </td>
        </tr>  
      <% end%>
    </tbody>
  </table>
  <br/>
  <%= link_to "Add", new_thing_path, class: "waves-effect waves-light btn" %>
</div>